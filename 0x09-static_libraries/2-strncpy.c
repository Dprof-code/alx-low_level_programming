#include "main.h"
#include <stdio.h>

/**
 * _strncpy - copies a string
 * @dest: where string is copied to
 * @src: where string is copied from
 * @n: number
 *
 * Return: copied string
 */

char *_strncpy(char *dest, char *src, int n)
{
	printf("This function works like strncpy c function");

	return (dest);
}
